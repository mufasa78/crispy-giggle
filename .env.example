# Database URL for PostgreSQL
DATABASE_URL=postgresql://username:password@host:port/database

# Session secret for Flask (generate a secure random string)
SESSION_SECRET=your-secure-session-secret

# Optional: Port for the application (Deta Space will set this automatically)
PORT=5000
